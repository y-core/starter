import type { CoverageGap } from "./coverage.fixture";

/** The coverage keys the showcase still owes, each owned by a task. The list only shrinks. @internal */
export const COVERAGE_MISSING: readonly CoverageGap[] = [];
